# Executor 能力缺口反馈台账

> 状态：Active
> 更新日期：2026-09-21

本台账记录 Aki 对 pinned `third_party/executor` 的能力缺口反馈，流程、分级与状态定义见
工程规范第 9.4 节。确认缺口前必须先核对当前 pinned 版本的公开头文件、API 文档、集成
指南与相关测试；只写“Executor 不支持”不构成有效记录。

## 台账

| 编号 | 日期 | 等级 | 状态 | 摘要 |
| --- | --- | --- | --- | --- |
| （暂无条目） | | | | |

## 记录要求

每条记录须包含：可复现证据、影响范围、期望语义、造成缺口的 API/语义限制、为什么现有
lifecycle 与 comm 设施不足、建议的最小 Executor 能力或获批例外、延期影响与可验收结果，
并在相关代码、测试或设计文档中引用编号。临时方案只能存在于单一 Adapter/compatibility
boundary 内，且不得创建线程、队列或调度器。
